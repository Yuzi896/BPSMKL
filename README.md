# BPS-MKL
A bayesain methods that can identify important groups, features within groups and interactions simultaneously.

# Installation
Using the 'devtools' package:

install.packages("devtools")

library(devtools)

install_github('Yuzi896/BPSMKL')

Usage and Example, see the pdf manual for more details.

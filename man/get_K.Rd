% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_K.R
\name{get_K}
\alias{get_K}
\title{Construct a dense kernel matrix for selected effects}
\usage{
get_K(X, Eta, lambda, update = FALSE, eta = NULL, i = NULL, j = NULL)
}
\arguments{
\item{X}{Numeric matrix with observations in rows and predictors in columns.}

\item{Eta}{Numeric square matrix indicating which main effects and
interactions are active. Diagonal entries represent main effects and
off-diagonal entries represent interactions.}

\item{lambda}{Numeric vector of length 2. \code{lambda[1]} is the bandwidth for
interaction terms and \code{lambda[2]} is the bandwidth for main effects.}

\item{update}{Logical flag. Use \code{FALSE} to compute the kernel from \code{Eta} as
supplied, or \code{TRUE} to compute it after temporarily updating the \verb{(i, j)}
and \verb{(j, i)} entries of \code{Eta}.}

\item{eta}{Proposed value to use when \code{update = TRUE}; must be \code{0} or \code{1}.
Defaults to \code{NULL}.}

\item{i}{One-based row index in \code{Eta} to update when \code{update = TRUE}.
Defaults to \code{NULL}.}

\item{j}{One-based column index in \code{Eta} to update when \code{update = TRUE}.
Defaults to \code{NULL}.}
}
\value{
An \code{N} by \code{N} numeric kernel matrix, where \code{N = nrow(X)}.
}
\description{
\code{get_K()} builds the \code{N} by \code{N} kernel matrix used by the model from an
input design matrix, an effect-selection matrix, and two bandwidth
parameters. The candidate main-effect and interaction index matrix is
computed internally from \code{Eta}.
}
\details{
Off-diagonal active entries in \code{Eta} are treated as interactions and use
\code{lambda[1]}; diagonal active entries are treated as main effects and use
\code{lambda[2]}. When \code{update = TRUE}, the C++ helper works with a temporary copy
of \code{Eta} where \code{Eta[i, j]} and \code{Eta[j, i]} are set to \code{eta}.
The candidate index matrix is always computed from the original \code{Eta}.
}

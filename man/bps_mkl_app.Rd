% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bps_mkl_app.R
\name{bps_mkl_app}
\alias{bps_mkl_app}
\title{Run approximate BPS-MKL from individual initial values}
\usage{
bps_mkl_app(
  iterations,
  burn,
  L,
  As,
  Y,
  X,
  m,
  P_eta = 0.2,
  P_gamma = 0.2,
  sigma2 = 0.1,
  lambdas = NULL,
  betas = NULL,
  gammas = NULL,
  Etas = NULL
)
}
\arguments{
\item{iterations}{Integer. Total number of MCMC iterations to run.}

\item{burn}{Integer. Number of burn-in iterations used for proposal
adaptation.}

\item{L}{Integer. Number of pathways.}

\item{As}{List of \code{L} numeric adjacency matrices. Each matrix defines the
candidate main effects and interactions for one pathway.}

\item{Y}{Numeric response vector or \verb{n x 1} matrix.}

\item{X}{Numeric \verb{n x p} design matrix, with observations in rows and
predictors in columns.}

\item{m}{Integer. Number of observations, or rows of \code{X}, to sample as
Nyström landmarks.}

\item{P_eta}{Numeric scalar. eta inclusion probability.}

\item{P_gamma}{Numeric scalar. gamma inclusion probability.}

\item{sigma2}{Numeric scalar. initial residual variance.}

\item{lambdas}{Numeric vector of length \code{2 * L}, with interaction and
main-effect bandwidths for each pathway.}

\item{betas}{Numeric vector of length \code{L} containing initial beta values.}

\item{gammas}{Numeric vector of length \code{L} containing initial gamma values.}

\item{Etas}{List of \code{L} numeric eta matrices.}
}
\value{
A list with MCMC draws and diagnostics:
\describe{
\item{\code{gamma_save}}{An \code{iterations} by \code{L} matrix of gamma draws.}
\item{\code{beta_save}}{An \code{iterations} by \code{L} matrix of beta draws.}
\item{\code{sigma_save}}{A numeric vector of sigma2 draws.}
\item{\code{eta_whole}}{A list of \code{L} matrices storing eta draws for the
candidate terms in each pathway.}
\item{\code{eta_idx}}{A list of \code{L} two-column matrices giving the one-based
predictor indices corresponding to the columns of \code{eta_whole}.}
\item{\code{lambda_save}}{An \code{iterations} by \code{2 * L} matrix of lambda draws.}
\item{\code{ll_save}}{A numeric vector of log-likelihood values.}
\item{\code{time}}{Time used to run the algorithm, returned
as a \code{difftime} object.}
}
}
\description{
\code{bps_mkl_app()} collects the individual initial values into the list expected
by the optimized C++ sampler and then runs the approximate Nyström BPS-MKL
algorithm.
}
\examples{
\dontrun{
# Assume simulation objects are already pre-generated.
# See details in the `bps_mkl()` example.
result <- bps_mkl_app(
  iterations = N,
  burn = burnin,
  L = L,
  As = A,
  Y = Y,
  X = X,
  m = 50,
  sigma2 = sigma2,
  betas = beta,
  gammas = gamma,
  Etas = A_imp
)
}

}

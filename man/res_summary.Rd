% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/res_summary.R
\name{res_summary}
\alias{res_summary}
\title{Summarize BPS-MKL MCMC results}
\usage{
res_summary(
  res,
  pathway = NULL,
  gammas = NULL,
  p = NULL,
  burnin = 0,
  Figure = FALSE
)
}
\arguments{
\item{res}{Result list returned by \code{bps_mkl()} or \code{bps_mkl_app()}.}

\item{pathway}{Optional simulated pathway object, such as the output from
\code{simulate_pathway()}. When supplied, \code{pathway$A_imp} is used as truth for
AUC calculation and optional figures.}

\item{gammas}{Optional numeric vector of true pathway inclusion indicators.
When supplied with \code{pathway} and \code{Figure = TRUE}, truth heatmaps multiply
\code{A_imp} by \code{gammas}.}

\item{p}{Optional integer number of predictors. If \code{NULL}, this is inferred
from \code{dim(res$eta_whole[[1]])[1]}.}

\item{burnin}{Integer number of initial MCMC draws to discard. Defaults to
\code{0}.}

\item{Figure}{Logical. If \code{TRUE}, return heatmap figures when \code{ggplot2} is
available. Defaults to \code{FALSE}.}
}
\value{
A list containing:
\describe{
\item{\code{idx_res}}{A data frame with \code{pathway_id}, \code{idx1}, \code{idx2},
\code{estimated}, and \code{P_pathway}. If \code{pathway} is supplied, it also contains
\code{truth}.}
\item{\code{auc_sim}}{A data frame with pathway-level AUC values. Returned only
when \code{pathway} is supplied.}
\item{\code{Figure}}{A list of pathway heatmaps. Returned only when
\code{Figure = TRUE} and \code{ggplot2} is available. If \code{pathway} is supplied,
truth heatmaps are included.}
}
}
\description{
\code{res_summary()} summarizes posterior pathway and eta inclusion probabilities
from a fitted BPS-MKL result object. It always returns an \code{idx_res} data
frame with pathway labels, predictor index pairs, and estimated eta
probabilities. When simulated truth is supplied through \code{pathway}, it also
returns pathway-level AUC values. When \code{Figure = TRUE} and \code{ggplot2} is
available, it also returns diagnostic heatmaps.
}
